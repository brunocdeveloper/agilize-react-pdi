import styled from "styled-components";
import {
  backgroundColor,
  color,
  display,
  justifyContent,
  space,
} from "styled-system";

export const Container: any = styled.div`
  ${display}
  ${justifyContent}
  ${color};
  ${space};
`;
